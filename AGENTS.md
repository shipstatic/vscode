# ship-vscode — agent instructions

Read `CLAUDE.md` (same directory). It is the single source of truth for this
repo's instructions; everything in it applies to every agent, not just
Claude. Do not duplicate its content here.
